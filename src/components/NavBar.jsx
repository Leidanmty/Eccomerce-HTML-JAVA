import React from 'react';
import { NavBar } from 'react-bootstrap';

const NavBar = () => {
    return (
        <NavBar>
            <Container>
                
            </Container>
        </NavBar>
    );
};

export default NavBar;